{
    // if set to true, it enables debug output into the sublime console
    "debug": false,

    // Choose the string that will appear inside the comment before the global values
    // ex: "globalString": "global" -> /*global ..., ..., ... */
    //     "globalString": "accessible" -> /*accesible ..., ..., ... */
    "globalString": "global",

    // choose the comment style for the global tag to appear in
    "commentStyle": {
        "begin": "/*",
        "end": " */",
        "onePerLine": false
    },

    // The seperator that will be used in between the defined globals
    "seperator": ",",

    // Have a popup to decide to add the global tag if it isn't already present
    "notifyIfMissing": false,

    // Have a popup to decide if ': true', ': false' should be added to the entry
    "indicateAssignment": true,
    // If indicateAssignment is false, this will decide if a default assignment will be set
    // false, "true", "false"
    "defaultAssignment": false,

    // Adds a new line after the global comment
    "addNewLine": true,

    // Scroll the global section at the top of the file into view
    "scrollGlobalsIntoView": false,

    "js": {
        // if set to true, it enables debug output into the sublime console
        "debug": false,

        // Choose the string that will appear inside the comment before the global values
        // ex: "globalString": "global" -> /*global ..., ..., ... */
        //     "globalString": "accessible" -> /*accesible ..., ..., ... */
        "globalString": "global",

        // choose the comment style for the global tag to appear in
        "commentStyle": {
            "begin": "/*",
            "end": " */",
            "onePerLine": false
        },

        // The seperator that will be used in between the defined globals
        "seperator": ",",

        // Have a popup to decide to add the global tag if it isn't already present
        "notifyIfMissing": false,

        // Have a popup to decide if ': true', ': false' should be added to the entry
        "indicateAssignment": true,
        // If indicateAssignment is false, this will decide if a default assignment will be set
        // false, "true", "false"
        "defaultAssignment": false,

        // Adds a new line after the global comment
        "addNewLine": true,

        //
        "scrollGlobalsIntoView": false
    }
}
